name: Deploy

on:
  workflow_dispatch:
  workflow_run:
    types:
      - completed
    workflows:
      - 'Lucky App CI'

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    run: |
      curl ${{ secrets.RENDER_DEPLOY_HOOK }}
